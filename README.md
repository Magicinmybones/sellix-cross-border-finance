# Sellix — Cross-border finance

Static landing page. Fonts and hero image extracted from inline base64 into `assets/`.

- `index.html` — page markup and styles
- `assets/` — PJS woff2 fonts (300/400/500/600/800) + hero webp
